import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    paddingTop: 10,
    paddingRight: 15,
    fontSize: 26,
    color: 'white',
    fontWeight: '500'
  }
});
export default styles;